"use client";
import { FormEvent, useState } from "react";
export function LoginForm(){const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage("Member login is being activated. Contact URO if you need access now.");setBusy(false);return;}const data=new FormData(e.currentTarget);const res=await fetch(`${url}/auth/v1/token?grant_type=password`,{method:"POST",headers:{apikey:key,"Content-Type":"application/json"},body:JSON.stringify({email:String(data.get("email")),password:String(data.get("password"))})});if(!res.ok){const body=await res.json().catch(()=>({}));setMessage(body.error_description||body.msg||"Unable to sign in.");setBusy(false);return;}const session=await res.json();sessionStorage.setItem("uro_session",JSON.stringify(session));window.location.href="/dashboard";}return <form className="form-card login-card" onSubmit={submit}><label>Email<input type="email" name="email" required autoComplete="email"/></label><label>Password<input type="password" name="password" required autoComplete="current-password"/></label><button className="button" disabled={busy}>{busy?"Signing in…":"Sign in"}</button>{message&&<p className="form-message fallback">{message}</p>}</form>}
