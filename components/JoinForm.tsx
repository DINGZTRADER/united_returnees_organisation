"use client";
import { FormEvent, useState } from "react";
import { SITE } from "@/lib/site";
export function JoinForm(){const[status,setStatus]=useState<"idle"|"sending"|"done"|"fallback">("idle");const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));setStatus("sending");try{const res=await fetch("/api/membership",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const body=await res.json().catch(()=>({}));if(res.ok){setStatus("done");setMessage("Application received. Continue to WhatsApp to complete registration and receive payment instructions.");return;}throw new Error(body.error||"Membership system is not connected yet.");}catch{setStatus("fallback");setMessage("Online applications are being activated. Your details can be sent directly to URO on WhatsApp now.");const summary=`Hello URO. I want to join URO.\nName: ${data.full_name||""}\nEmail: ${data.email||""}\nPhone: ${data.phone||""}\nCurrent country: ${data.current_country||""}\nReturn status: ${data.return_status||""}\nSupport needed: ${data.support_needs||""}`;window.open(`https://wa.me/${SITE.whatsapp}?text=${encodeURIComponent(summary)}`,"_blank","noopener,noreferrer");}}return <form className="form-card" onSubmit={submit}><div className="form-grid"><label>Full name<input name="full_name" required autoComplete="name"/></label><label>Email address<input name="email" type="email" required autoComplete="email"/></label><label>Phone / WhatsApp<input name="phone" required autoComplete="tel"/></label><label>Current country<input name="current_country" required/></label><label>Return status<select name="return_status" required defaultValue=""><option value="" disabled>Select one</option><option value="planning">Planning to return</option><option value="returned">Already returned</option></select></label><label>Expected / actual return date<input name="return_date" type="date"/></label><label>District in Uganda<input name="district"/></label><label>Professional background<input name="professional_background"/></label></div><label>Skills and experience<textarea name="skills" rows={3} placeholder="Tell us the skills, qualifications or experience you are bringing home."/></label><label>What support are you looking for?<textarea name="support_needs" rows={4} required placeholder="Employment, business, investment, housing, schools, government services, mentorship…"/></label><label className="check-row"><input type="checkbox" name="consent" value="yes" required/><span>I consent to URO using this information to assess my membership and support needs.</span></label><button className="button" disabled={status==="sending"}>{status==="sending"?"Submitting…":`Apply for Membership — $${SITE.annualFeeUsd}/year`}</button>{message&&<p className={`form-message ${status}`}>{message}</p>}{status==="done"&&<a className="button button-secondary" href={`https://wa.me/${SITE.whatsapp}?text=${encodeURIComponent("Hello URO. I have submitted my membership application and would like to complete registration and payment.")}`} target="_blank" rel="noreferrer">Continue on WhatsApp</a>}</form>}
